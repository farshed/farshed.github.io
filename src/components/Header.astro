---
const pathname = Astro.url.pathname.replaceAll(/^\/+|\/+$/g, '');
const links = {
  // projects: 'Projects',
  blog: 'Blog'
};
---

<header>
  <h1>
    <a href="/">Faisal Arshed</a>
  </h1>
  <nav>
    <ul>
      {
        Object.entries(links).map(([path, label]) => (
          <li>
            <a href={`/${path}`} class={path === pathname ? 'active' : ''}>
              {label}
            </a>
          </li>
        ))
      }
      <li>
        <div id="sun">
          <div></div>
        </div>
      </li>
    </ul>
  </nav>
</header>
